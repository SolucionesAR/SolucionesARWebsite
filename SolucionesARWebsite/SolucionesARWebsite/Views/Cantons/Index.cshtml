@using SolucionesARWebsite.ModelsWebsite.Views.Cantons

@model IndexViewModel


@{
    ViewBag.Title = "SolucionesAR | Cantones";
    Layout = "~/Views/Shared/TableLayout.cshtml";
}

@section Messages {
}

@section ListTitle {
    CANTONES
}

@section ListActions {
    @if (Model.CantonsList.HasFilters())
    {
        @Html.ActionLink("Mostrar Todo", "Index", null, new { @class = "button" } )
    }
        @Html.ActionLink("Nuevo Canton", "Create", null, new { @class = "button" })
    }
}

@section ListContent {
    <div class="entry jspScrollable">
        <div class="table_style">
            <table>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Nombre
                    </th>
                    <th>
                        Provincia
                    </th>
                    
                    <th></th>
                </tr>

                @foreach (var item in Model.CantonsList.Items) {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CantonId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ProvinceId)
                        </td>
                        
                        <td>
                            @Html.ActionLink("Editar", "Edit", new { id = item.CantonId }) |
                            @Html.ActionLink("Detalles", "Details", new { id = item.CantonId }) 
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
}

@section ListPaging {
    @{
        /*
        @Html.DropDownListForPaging(4, 5, Model.RouteValues)
        var totalLabel = Model.UsersList.TotalItems != 1 ? "users" : "user";
        @ListHelper.TotalItems(Model.UsersList.TotalItems, totalLabel)
        @ListHelper.Paging(Url, Model.UsersList)
        */
    }
}