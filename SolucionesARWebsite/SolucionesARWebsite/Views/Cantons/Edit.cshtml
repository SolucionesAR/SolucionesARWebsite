
@using SolucionesARWebsite.ModelsWebsite.Views.Cantons

@model EditViewModel

@{
    ViewBag.Title = "SolucionesAR | Cantones";
    Layout = "~/Views/Shared/EditionLayout.cshtml";
}

@section Messages {

}


@section EditionContent {
    <div class="edition_content content_box ">
        
        @if (Model.CantonId > 0)
        {
            <h1>EDITAR CANTON</h1>
        }
        else
        {
            <h1>NUEVO CANTON</h1>
        }

        <div class="entry jspScrollable">
            <p class="note">Favor complete el siguiente formulario</p>
            <div class="jspContainer" style="width: 960px; height: 400px;">
                <div class="jspPane" style="padding: 15px 25px 0px; top: 0px;">
                    @using (Html.BeginForm("Save", "Cantons"))
                    {
                        @Html.HiddenFor(model => model.CantonId)
                        <fieldset>
                            @Html.EditorFor(model => model.CantonName)
                            @Html.EditorFor(model=> model.ProvinceId)
                        </fieldset>
                            
                        <input name="submit" type="submit" value="Submit">
                    }
                </div>
            </div>
        </div>
    </div>
}

