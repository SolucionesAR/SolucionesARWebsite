@using PagedList.Mvc
@using SolucionesARWebsite2.ViewModels.IdentificationTypes

@model IndexViewModel

@{
    ViewBag.Title = "SolucionesAR | Tipos de Identificacion";
    Layout = "~/Views/Shared/TableLayout.cshtml";
}

@section Messages {
}

@section ListTitle {
    TIPOS DE IDENTIFICACION
}

@section ListPaging {
    @{      
        @Html.PagedListPager(Model.PagedItems,
            page => Url.Action("Index", new RouteValueDictionary {
               { "Page", page },
               { "EmailAddress", Model.EmailAddress },
               { "LastName", Model.LastName }
            }),
            PagedListRenderOptions.OnlyShowFivePagesAtATime)
    }   
}

@section ListActions {
        @Html.ActionLink("Nuevo Tipo de Identificacion", "Create", null, new { @class = "button" })
    }
}

@section ListContent {
    <div class="entry jspScrollable">
        <div class="table_style">
            <table>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Nombre
                    </th>
                    
                    <th></th>
                </tr>

                @foreach (var item in Model.PagedItems) {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.IdentificationTypeId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.IdentificationDescription)
                        </td>
                        
                        <td>
                            @Html.ActionLink("Editar", "Edit", new { id = item.IdentificationTypeId })
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
}