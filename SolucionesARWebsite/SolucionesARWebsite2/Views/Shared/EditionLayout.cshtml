@using SolucionesARWebsite2.ViewModels

@model BaseViewModel
           
@{
    Layout = "~/Views/Shared/MasterLayout.cshtml";
}

@section PageContent {
    <div class="edition_content">
    @{
        @RenderSection("Header", true)

        @RenderSection("Messages", true)

        @RenderSection("EditionContent", true)
    }
    </div>
}